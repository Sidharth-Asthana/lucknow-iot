FROM python:3.12-slim

# No cache busting needed — paho-mqtt is stable
RUN pip install --no-cache-dir paho-mqtt==1.6.1

WORKDIR /app
COPY simulator.py /app/simulator.py

CMD ["python3", "-u", "/app/simulator.py"]
